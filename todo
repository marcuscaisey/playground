* Mention installation wrt completion script examples / instructions
* CI test checking that markdown manual is in sync
* Run goreleaser build in CI
* Set up goreleaser releases in CI
* Set up built-in templates so that 3rd party deps can be installed easily
